{
  "name": "personal-hub",
  "version": "1.0.0",
  "private": true,
  "description": "EryyynIT personal hub — static one-page site pre-rendered from data/content.js at build time",
  "scripts": {
    "build": "node scripts/generate-site.js",
    "pretest": "node scripts/generate-site.js",
    "test": "node test/smoke.test.js && node test/lang-redirect.test.js && node test/resume.test.js && node test/about.test.js && node test/game.test.js && node test/nav-state.test.js"
  },
  "engines": {
    "node": ">=14"
  }
}
